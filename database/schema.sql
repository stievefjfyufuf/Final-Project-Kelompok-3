-- DROP TABLE (biar bisa rerun tanpa error)
BEGIN EXECUTE IMMEDIATE 'DROP TABLE assignments CASCADE CONSTRAINTS'; EXCEPTION WHEN OTHERS THEN NULL; END;
/
BEGIN EXECUTE IMMEDIATE 'DROP TABLE schedules CASCADE CONSTRAINTS'; EXCEPTION WHEN OTHERS THEN NULL; END;
/
BEGIN EXECUTE IMMEDIATE 'DROP TABLE users CASCADE CONSTRAINTS'; EXCEPTION WHEN OTHERS THEN NULL; END;
/

-- USERS
CREATE TABLE users (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(100)
);

-- ASSIGNMENTS
CREATE TABLE assignments (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id NUMBER,
    title VARCHAR2(255),
    description VARCHAR2(500),
    deadline TIMESTAMP,
    status VARCHAR2(50),

    CONSTRAINT fk_user_assignments
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- SCHEDULES
CREATE TABLE schedules (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id NUMBER,
    title VARCHAR2(255),
    start_time TIMESTAMP,
    end_time TIMESTAMP,

    CONSTRAINT fk_user_schedules
    FOREIGN KEY (user_id) REFERENCES users(id)
);